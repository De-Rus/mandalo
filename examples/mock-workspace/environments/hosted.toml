name = "hosted"

[vars]
baseUrl = "https://api.mandalo.dev"
wsUrl = "wss://api.mandalo.dev"
mqttUrl = "wss://api.mandalo.dev:8884"
trace = "hosted"
room = "kitchen"
# TODO: point this at the hosted mock's gRPC endpoint once it is deployed.
# Until then it is deliberately absent: a "hosted" gRPC request fails loud with
# `unresolved variable: grpcUrl` instead of quietly calling the local mock.
# grpcUrl = "<the hosted gRPC url>"
