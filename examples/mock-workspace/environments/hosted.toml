name = "hosted"

[vars]
baseUrl = "https://api.mandalo.dev"
wsUrl = "wss://api.mandalo.dev"
mqttUrl = "wss://api.mandalo.dev:8884"
trace = "hosted"
room = "kitchen"
# The hosted mock serves gRPC on its own port: Fly terminates TLS there and
# passes HTTP/2 through to the server, which is what gRPC needs end to end.
grpcUrl = "https://api.mandalo.dev:8443"
