# Swagger Petstore

This folder is the OpenAPI document only (`openapi.json`). Opening it as a
workspace in the desktop app scaffolds a local `mandalo.toml` + imported
requests (gitignored). Or import the spec into any other workspace:

```sh
mandalo --workspace ~/Mandalo import examples/petstore/openapi.json
```
