name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  # ---------------------------------------------------------------- desktop
  # tauri-action creates the draft release on its first run and uploads the
  # installers into it. Everything else attaches to the same draft afterwards.
  desktop:
    name: Desktop ${{ matrix.label }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: macOS (Apple silicon)
            platform: macos-14
            args: --target aarch64-apple-darwin
            sidecar: aarch64-apple-darwin
          - label: macOS (Intel)
            platform: macos-14
            args: --target x86_64-apple-darwin
            sidecar: x86_64-apple-darwin
          - label: macOS (universal)
            platform: macos-14
            args: --target universal-apple-darwin
            sidecar: universal-apple-darwin
          - label: Linux
            platform: ubuntu-22.04
            args: ""
            sidecar: x86_64-unknown-linux-gnu
          - label: Windows
            platform: windows-latest
            args: ""
            sidecar: x86_64-pc-windows-msvc
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      - name: Install Tauri system deps (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf \
            pkg-config libssl-dev cmake
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin
      - uses: Swatinem/rust-cache@v2
      - run: pnpm install --frozen-lockfile
      # The bundle carries the CLI as a Tauri sidecar, which is what lets the
      # Homebrew cask link one `mandalo` out of the installed .app. Without this
      # step `tauri build` fails: externalBin is declared in tauri.conf.json.
      - name: Stage the CLI sidecar
        shell: bash
        run: bash scripts/build-sidecar.sh ${{ matrix.sidecar }}
      - name: Apple credentials
        id: apple
        if: runner.os == 'macOS'
        uses: ./.github/actions/apple-credentials
        with:
          certificate: ${{ secrets.APPLE_CERTIFICATE }}
          api-key-p8: ${{ secrets.APPLE_API_KEY_P8 }}
          required: ${{ startsWith(github.ref, 'refs/tags/v') }}
      # tauri-action signs the .app and .dmg from APPLE_CERTIFICATE and notarizes
      # the .app from the APPLE_API_* trio; with none of them set it just builds
      # unsigned. It does NOT notarize the .dmg — see the step after it.
      - uses: tauri-apps/tauri-action@v0
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Mándalo ${{ github.ref_name }}"
          releaseDraft: true
          updaterJsonPreferNsis: true
          args: ${{ matrix.args }}
      # Tauri notarizes and staples the .app but leaves the .dmg carrying it
      # merely signed, and Gatekeeper assesses the downloaded .dmg on its own:
      # `spctl -a -t open` on it reports "Unnotarized Developer ID" and refuses
      # to open it, however notarized the app inside is. So notarize the disk
      # image too and put the stapled copy back on the release, before the
      # publish job hashes what is there into SHA256SUMS.
      - name: Notarize and staple the DMG
        if: steps.apple.outputs.sign == 'true' && startsWith(github.ref, 'refs/tags/v')
        shell: bash
        env:
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          shopt -s nullglob
          dmgs=(target/*/release/bundle/dmg/*.dmg)
          if [ ${#dmgs[@]} -eq 0 ]; then
            echo "::error::tauri-action produced no .dmg for ${{ matrix.label }}"
            exit 1
          fi
          for dmg in "${dmgs[@]}"; do
            bash scripts/macos-notarize.sh "$dmg"
            spctl -a -t open --context context:primary-signature -v "$dmg"
            gh release upload "${{ github.ref_name }}" "$dmg" --clobber
          done
      # workflow_dispatch has no tag to release into, so it only proves it builds.
      - uses: tauri-apps/tauri-action@v0
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          args: ${{ matrix.args }}

  # -------------------------------------------------------------------- cli
  cli:
    name: CLI ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-14
          - target: x86_64-apple-darwin
            os: macos-14
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-22.04-arm
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Install CLI build deps (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev cmake
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
      - name: Build
        run: cargo build --profile release-cli -p mandalo-cli --target ${{ matrix.target }}
      - name: Apple credentials
        id: apple
        if: runner.os == 'macOS'
        uses: ./.github/actions/apple-credentials
        with:
          certificate: ${{ secrets.APPLE_CERTIFICATE }}
          api-key-p8: ${{ secrets.APPLE_API_KEY_P8 }}
          required: ${{ startsWith(github.ref, 'refs/tags/v') }}
      # Signing has to happen before packaging: the tar.gz must carry the signed
      # binary, and notarytool cannot read a tar.gz anyway.
      - name: Sign and notarize the binary (macOS)
        if: steps.apple.outputs.sign == 'true'
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
        run: |
          binary=target/${{ matrix.target }}/release-cli/mandalo
          bash scripts/macos-sign.sh "$binary"
          bash scripts/macos-notarize.sh "$binary"
      - name: Package
        id: pkg
        shell: bash
        env:
          TARGET: ${{ matrix.target }}
        run: bash scripts/package-cli.sh "$TARGET" "$GITHUB_REF_NAME" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-artifact@v4
        with:
          name: cli-${{ matrix.target }}
          path: ${{ steps.pkg.outputs.asset }}
          if-no-files-found: error

  # -------------------------------------------------------------- extension
  # Platform VSIXs bundle the matching CLI binary; the "universal" one carries no
  # binary, so HTTP and GraphQL run in-process and gRPC asks for the CLI.
  # MANDALO_CLI_TARGET is set for the whole job because `vsce package` re-runs
  # `vscode:prepublish` -> fetch-cli.mjs, which would otherwise assume the host
  # and overwrite a cross-built binary with the runner's own architecture.
  extension:
    name: Extension ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: darwin-arm64
            os: macos-14
          - target: darwin-x64
            os: macos-14
            rust-target: x86_64-apple-darwin
          - target: linux-x64
            os: ubuntu-22.04
          - target: linux-arm64
            os: ubuntu-22.04-arm
          - target: win32-x64
            os: windows-latest
          - target: universal
            os: ubuntu-22.04
    runs-on: ${{ matrix.os }}
    env:
      MANDALO_CLI_TARGET: ${{ matrix.target }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install CLI build deps (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev cmake
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-target }}
      - uses: Swatinem/rust-cache@v2
      - run: pnpm install --frozen-lockfile
        working-directory: editor-extension
      # The request editor webview is built from the root app. Without this step
      # the custom editor ships empty.
      - name: Build the request-editor webview
        run: |
          pnpm install --frozen-lockfile
          pnpm run build:vscode
      - name: Read extension version
        id: meta
        shell: bash
        run: echo "version=$(node -p "require('./editor-extension/package.json').version")" >> "$GITHUB_OUTPUT"
      - name: Fetch or build the CLI
        working-directory: editor-extension
        run: node scripts/fetch-cli.mjs --target ${{ matrix.target }} --build
      - name: Apple credentials
        id: apple
        if: startsWith(matrix.target, 'darwin-')
        uses: ./.github/actions/apple-credentials
        with:
          certificate: ${{ secrets.APPLE_CERTIFICATE }}
          api-key-p8: ${{ secrets.APPLE_API_KEY_P8 }}
          required: ${{ startsWith(github.ref, 'refs/tags/v') }}
      # The binary inside the VSIX is the one that actually runs on the user's
      # machine, so it needs the same Developer ID signature as the standalone CLI.
      - name: Sign and notarize the bundled CLI (macOS)
        if: steps.apple.outputs.sign == 'true'
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
        run: |
          bash scripts/macos-sign.sh editor-extension/bin/mandalo
          bash scripts/macos-notarize.sh editor-extension/bin/mandalo
      - name: The VSIX carries the binary it claims to
        if: matrix.target != 'universal'
        working-directory: editor-extension
        shell: bash
        run: test -s "bin/mandalo" || test -s "bin/mandalo.exe"
      - name: Package VSIX
        working-directory: editor-extension
        shell: bash
        run: |
          set -euo pipefail
          out="mandalo-${{ matrix.target }}-${{ steps.meta.outputs.version }}.vsix"
          if [ "${{ matrix.target }}" = "universal" ]; then
            npx @vscode/vsce package --no-dependencies -o "$out"
          else
            npx @vscode/vsce package --target "${{ matrix.target }}" --no-dependencies -o "$out"
          fi
          ls -l "$out"
      - uses: actions/upload-artifact@v4
        with:
          name: vsix-${{ matrix.target }}
          path: editor-extension/mandalo-${{ matrix.target }}-${{ steps.meta.outputs.version }}.vsix
          if-no-files-found: error

  # ---------------------------------------------------------------- publish
  # Everything lands on the one draft release: the desktop installers tauri-action
  # already pushed, plus the CLI archives, the VSIXs, and a SHA256SUMS covering
  # all of them.
  publish:
    name: Attach assets and write the notes
    needs: [desktop, cli, extension]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      TAG: ${{ github.ref_name }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: incoming
          pattern: cli-*
          merge-multiple: true
      - uses: actions/download-artifact@v4
        with:
          path: incoming
          pattern: vsix-*
          merge-multiple: true
      - name: Collect the desktop installers already on the release
        run: |
          set -euo pipefail
          mkdir -p incoming
          gh release download "$TAG" --dir incoming --clobber \
            --pattern '*.dmg' --pattern '*.app.tar.gz' --pattern '*.msi' \
            --pattern '*-setup.exe' --pattern '*.AppImage' --pattern '*.deb' \
            --pattern '*.rpm' --pattern '*.sig' ||
            echo "no desktop installers matched — SHA256SUMS covers what is here"
          ls -l incoming
      - name: SHA256SUMS
        working-directory: incoming
        run: |
          set -euo pipefail
          rm -f SHA256SUMS
          # The redirection creates SHA256SUMS before find runs, so it must exclude itself.
          # shellcheck disable=SC2094  # -name SHA256SUMS is exactly that exclusion
          find . -maxdepth 1 -type f ! -name SHA256SUMS -printf '%f\0' |
            sort -z | xargs -0 sha256sum > SHA256SUMS
          cat SHA256SUMS
      - name: Release notes
        run: bash scripts/release-notes.sh "$TAG" incoming > notes.md && cat notes.md
      - name: Upload
        run: |
          set -euo pipefail
          gh release upload "$TAG" incoming/* --clobber
          gh release edit "$TAG" --notes-file notes.md --draft
          echo "The release is still a DRAFT. Publish it or /releases/latest/download/ and install.sh will not resolve."

  # Publishing to the Marketplace is optional: without VSCE_PAT the release is
  # already complete, the VSIXs are simply downloaded instead of installed.
  marketplace:
    name: Publish to the VS Code Marketplace
    needs: [publish]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    env:
      VSCE_PAT: ${{ secrets.VSCE_PAT }}
    steps:
      - name: Detect Marketplace credentials
        id: creds
        run: |
          if [ -n "${VSCE_PAT:-}" ]; then
            echo "publish=true" >> "$GITHUB_OUTPUT"
          else
            echo "publish=false" >> "$GITHUB_OUTPUT"
            echo "VSCE_PAT is not set — skipping the Marketplace publish. The VSIXs are on the release."
          fi
      - uses: actions/download-artifact@v4
        if: steps.creds.outputs.publish == 'true'
        with:
          path: vsix
          pattern: vsix-*
          merge-multiple: true
      - uses: actions/setup-node@v4
        if: steps.creds.outputs.publish == 'true'
        with:
          node-version: 22
      - name: Publish every platform VSIX
        if: steps.creds.outputs.publish == 'true'
        run: |
          set -euo pipefail
          for f in vsix/*.vsix; do
            npx @vscode/vsce publish --packagePath "$f"
          done
